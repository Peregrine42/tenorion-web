<!DOCTYPE html>
<html>
  <head>
    <title>Tenorion</title>
    <meta name="viewport" content="width=960, user-scalable=yes" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body class="full-height d-flex align-items-stretch flex-column">
    <div class="container d-flex flex-column align-items-stretch full-height">
      <%= yield %>
      <div class="mt-3 mb-3 footer d-flex align-items-spread justify-content-between container">
        <a class="border btn rounded-top btn-outline-secondary" href="https://github.com/Peregrine42">© 2023 Duncan Fermor</a>
        <% if not signed_in? %>
          <a class="border btn rounded-top btn-outline-secondary text-align-right" href="/sign-in">Sign in</a>
        <% else %>
          <%= simple_form_for :session, :url => sign_out_path do %>
            <button class="border btn rounded-top btn-outline-secondary text-align-right">Sign out</button>
          <% end %>
        <% end %>
      </div>
    </div>
  </body>
</html>
